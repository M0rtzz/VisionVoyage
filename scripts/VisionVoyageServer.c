#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -f VisionVoyageServer.sh -o VisionVoyageServer && mv VisionVoyageServer.sh.c VisionVoyageServer.c && rm -f VisionVoyageServer
#endif

static char data[] =
#define msg2_z 19
#define msg2 ((&data[0]))
	"\126\375\344\302\337\006\133\120\343\141\057\346\332\106\307\003"
	"\202\144\007\355\200\352\265"
#define text_z 450
#define text ((&data[47]))
	"\214\365\253\246\010\222\166\050\011\234\214\152\036\227\124\334"
	"\221\233\135\233\253\005\253\244\116\326\051\205\303\262\156\172"
	"\060\373\143\165\206\241\147\025\351\226\041\041\017\220\052\042"
	"\271\107\276\210\244\216\216\161\210\020\237\223\140\113\137\235"
	"\076\010\027\077\156\156\162\013\155\113\171\350\110\120\275\367"
	"\060\004\055\304\051\045\111\242\236\303\035\055\256\143\351\330"
	"\307\301\013\356\004\330\101\035\005\220\031\252\155\074\326\342"
	"\227\177\175\352\217\141\222\002\323\321\100\366\126\206\306\333"
	"\263\351\030\207\100\047\330\060\135\371\351\162\154\006\345\132"
	"\321\351\145\321\116\211\023\347\250\075\147\014\035\235\123\273"
	"\145\014\211\250\142\161\010\315\263\106\134\224\057\174\262\137"
	"\251\221\144\134\124\142\304\370\077\277\256\162\323\320\176\054"
	"\237\341\055\371\272\017\011\103\026\304\325\341\345\072\362\131"
	"\146\155\347\143\205\116\340\017\122\350\017\165\211\161\200\374"
	"\115\271\136\252\142\242\360\362\030\275\370\254\057\173\047\020"
	"\040\111\171\302\316\170\225\100\235\130\107\135\013\242\376\320"
	"\250\310\332\204\360\276\343\200\170\123\337\346\146\256\057\261"
	"\261\104\215\073\206\127\016\346\056\114\106\340\052\112\155\364"
	"\355\375\122\163\210\343\104\317\072\070\344\305\116\077\333\020"
	"\372\076\140\310\254\004\373\024\353\367\012\131\032\171\065\052"
	"\343\134\232\371\234\077\256\012\237\314\312\141\253\251\377\314"
	"\106\302\031\124\166\026\016\341\037\262\134\220\303\257\236\273"
	"\176\223\253\350\005\240\274\255\236\140\212\357\052\352\123\273"
	"\112\263\046\206\224\210\321\367\372\064\024\114\035\313\037\146"
	"\017\267\367\346\064\253\016\127\165\336\245\361\214\113\153\325"
	"\270\062\016\234\005\215\277\054\136\170\072\067\036\202\307\343"
	"\365\032\034\310\171\035\254\300\107\114\041\106\271\126\261\332"
	"\235\327\005\363\205\132\162\132\317\326\233\025\032\255\355\315"
	"\240\230\233\356\147\242\031\126\025\025\112\152\216\113\022\264"
	"\304\067\052\121\102\055\044\207\347\152\363\053\216\250\000\306"
	"\230\215\273\104\064\303\327\252\354\341\107\171\114\145\020\240"
	"\102\242\073\237\076\347\245\351\213\231\025\032\102\026\340\332"
	"\243\233\037\330\137\366\202\114\330\311\305\044\057\325\304\161"
	"\170\000\021\266\347\267\240\163\120\266\215\222\314\156\155\160"
	"\012\215\110\151\204\312\265\134\224\172\200\303\120\105\065\311"
	"\105\107\177\055\376\040\241\116\326\057\340\242\235\116\023\247"
	"\333\133\020"
#define xecc_z 15
#define xecc ((&data[602]))
	"\013\271\331\147\335\221\073\347\045\215\113\011\143\020\050\273"
	"\273\101"
#define shll_z 14
#define shll ((&data[621]))
	"\200\030\315\322\022\116\106\063\060\202\232\251\022\201\334\264"
	"\133"
#define opts_z 1
#define opts ((&data[637]))
	"\247"
#define chk2_z 19
#define chk2 ((&data[642]))
	"\225\110\321\304\110\260\165\220\053\126\214\324\121\067\032\135"
	"\334\152\102\353\011\037\161\051\164\142\167"
#define tst1_z 22
#define tst1 ((&data[665]))
	"\055\030\304\352\017\205\346\236\020\025\326\212\040\233\301\016"
	"\270\303\303\120\267\136\343"
#define msg1_z 65
#define msg1 ((&data[699]))
	"\340\154\305\042\250\104\264\051\371\020\357\352\156\017\376\341"
	"\107\123\144\216\364\250\127\110\344\205\006\105\021\376\245\324"
	"\167\346\370\117\374\245\350\012\150\324\203\230\166\002\166\112"
	"\117\021\377\336\023\374\275\361\024\223\107\300\133\370\212\242"
	"\265\067\041\025\252\306\320\053\217\263\037\226"
#define pswd_z 256
#define pswd ((&data[823]))
	"\357\347\171\070\270\076\142\055\240\331\264\252\054\230\304\335"
	"\243\245\112\151\307\363\256\174\034\247\215\014\235\170\360\215"
	"\140\152\305\030\251\050\105\111\001\372\363\055\223\270\013\066"
	"\135\126\237\045\111\115\241\146\365\056\162\033\323\074\055\333"
	"\104\117\365\221\243\375\366\366\274\267\233\232\032\164\114\233"
	"\070\212\253\330\254\162\007\172\301\034\225\224\131\303\157\236"
	"\023\145\060\266\143\046\254\037\336\110\272\371\275\007\225\365"
	"\222\100\316\076\263\325\271\164\362\117\011\113\022\170\351\045"
	"\336\032\334\101\100\211\141\037\321\033\030\216\043\255\204\265"
	"\356\122\364\241\050\255\025\032\374\037\145\017\230\117\065\166"
	"\151\021\270\252\232\031\312\154\065\343\372\130\220\177\015\176"
	"\321\001\040\372\256\066\024\253\125\172\272\355\312\357\143\064"
	"\001\033\337\233\065\251\007\152\214\002\302\035\201\317\234\123"
	"\321\274\115\200\362\142\053\107\334\346\064\247\326\230\333\327"
	"\263\272\163\350\144\173\122\360\175\025\015\377\345\251\123\266"
	"\146\241\067\130\003\142\237\340\111\324\207\037\154\143\366\037"
	"\036\151\010\202\344\133\163\142\160\200\142\125\052\265\014\220"
	"\127\103\351\133\246\210\073\357\135\303\017\311\047\006\351\105"
	"\160\361\307\124\114\072\267\275\273\031\023\346\317\040\167\046"
	"\143\140\201\012\351\275\372\106\201\011\017\223\247\142\040\007"
	"\315\346\040\166\016\146\300\020\141\263\076\364\154\111\053\311"
	"\240\313\356\351\030\220\117\016\277\302\241\147\045\302\157\362"
	"\250\217\150\267\366\050\307\130\334\005\114\110\117\170\022\357"
	"\103\001\331\134"
#define chk1_z 22
#define chk1 ((&data[1136]))
	"\135\277\250\351\172\176\315\240\007\355\173\223\247\015\020\242"
	"\234\264\154\046\361\020\121\353"
#define date_z 1
#define date ((&data[1160]))
	"\173"
#define tst2_z 19
#define tst2 ((&data[1161]))
	"\356\062\102\030\027\141\016\376\001\223\351\163\250\140\007\301"
	"\253\356\156"
#define lsto_z 1
#define lsto ((&data[1180]))
	"\313"
#define inlo_z 3
#define inlo ((&data[1181]))
	"\254\015\021"
#define rlax_z 1
#define rlax ((&data[1184]))
	"\120" /* End of data[] */;
#define hide_z 4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC 0 /* Define as 1 to debug execvp calls */
#define TRACEABLE 1 /* Define as 1 to enable ptrace the executable */
#define HARDENING 0 /* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON 0 /* Define as 1 to enable work with busybox */

#if HARDENING
static const char *shc_x[] = {
	"/*",
	" * Copyright 2019 - Intika <intika@librefox.org>",
	" * Replace ******** with secret read from fd 21",
	" * Also change arguments location of sub commands (sh script commands)",
	" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
	" */",
	"",
	"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
	"#define PLACEHOLDER \"********\"",
	"#include <dlfcn.h>",
	"#include <stdlib.h>",
	"#include <string.h>",
	"#include <unistd.h>",
	"#include <stdio.h>",
	"#include <signal.h>",
	"",
	"static char secret[128000]; //max size",
	"typedef int (*pfi)(int, char **, char **);",
	"static pfi real_main;",
	"",
	"// copy argv to new location",
	"char **copyargs(int argc, char** argv){",
	"    char **newargv = malloc((argc+1)*sizeof(*argv));",
	"    char *from,*to;",
	"    int i,len;",
	"",
	"    for(i = 0; i<argc; i++){",
	"        from = argv[i];",
	"        len = strlen(from)+1;",
	"        to = malloc(len);",
	"        memcpy(to,from,len);",
	"        // zap old argv space",
	"        memset(from,'\\0',len);",
	"        newargv[i] = to;",
	"        argv[i] = 0;",
	"    }",
	"    newargv[argc] = 0;",
	"    return newargv;",
	"}",
	"",
	"static int mymain(int argc, char** argv, char** env) {",
	"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
	"    return real_main(argc, copyargs(argc,argv), env);",
	"}",
	"",
	"int __libc_start_main(int (*main) (int, char**, char**),",
	"                      int argc,",
	"                      char **argv,",
	"                      void (*init) (void),",
	"                      void (*fini)(void),",
	"                      void (*rtld_fini)(void),",
	"                      void (*stack_end)){",
	"    static int (*real___libc_start_main)() = NULL;",
	"    int n;",
	"",
	"    if (!real___libc_start_main) {",
	"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
	"        if (!real___libc_start_main) abort();",
	"    }",
	"",
	"    n = read(21, secret, sizeof(secret));",
	"    if (n > 0) {",
	"      int i;",
	"",
	"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
	"    for (i = 1; i < argc; i++)",
	"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
	"          argv[i] = secret;",
	"    }",
	"",
	"    real_main = main;",
	"",
	"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
	"}",
	"",
	0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte.
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do
	{
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once.
 */
void key(void *str, int len)
{
	unsigned char tmp, *ptr = (unsigned char *)str;
	while (len > 0)
	{
		do
		{
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data.
 */
void arc4(void *str, int len)
{
	unsigned char tmp, *ptr = (unsigned char *)str;
	while (len > 0)
	{
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall)                                        \
	BPF_JUMP(BPF_JMP + BPF_JEQ + BPF_K, SYS_##syscall, 0, 1), \
		BPF_STMT(BPF_RET + BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
	/* validate arch */
	BPF_STMT(BPF_LD + BPF_W + BPF_ABS, ArchField),
	BPF_JUMP(BPF_JMP + BPF_JEQ + BPF_K, AUDIT_ARCH_X86_64, 1, 0),
	BPF_STMT(BPF_RET + BPF_K, SECCOMP_RET_KILL),

	/* load syscall */
	BPF_STMT(BPF_LD + BPF_W + BPF_ABS, offsetof(struct seccomp_data, nr)),

	/* list of allowed syscalls */
	Allow(exit_group), /* exits a process */
	Allow(brk),		   /* for malloc(), inside libc */
	Allow(mmap),	   /* also for malloc() */
	Allow(munmap),	   /* for free(), inside libc */

	/* and if we don't match above, die */
	BPF_STMT(BPF_RET + BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
	.len = sizeof(filter) / sizeof(filter[0]),
	.filter = filter};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening()
{
	if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0))
	{
		perror("Could not start seccomp:");
		exit(1);
	}
	if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1)
	{
		perror("Could not start seccomp:");
		exit(1);
	}
}
/* End Seccomp Sandboxing Init */

void shc_x_file()
{
	FILE *fp;
	int line = 0;

	if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL)
	{
		exit(1);
		exit(1);
	}
	for (line = 0; shc_x[line]; line++)
		fprintf(fp, "%s\n", shc_x[line]);
	fflush(fp);
	fclose(fp);
}

int make()
{
	char *cc, *cflags, *ldflags;
	char cmd[4096];

	cc = getenv("CC");
	if (!cc)
		cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd))
	{
		remove("/tmp/shc_x.c");
		return -1;
	}
	remove("/tmp/shc_x.c");
	return 0;
}

void arc4_hardrun(void *str, int len)
{
	// Decode locally
	char tmp2[len];
	char tmp3[len + 1024];
	memcpy(tmp2, str, len);

	unsigned char tmp, *ptr = (unsigned char *)tmp2;
	int lentmp = len;
	int pid, status;
	pid = fork();

	shc_x_file();
	if (make())
	{
		exit(1);
	}

	setenv("LD_PRELOAD", "/tmp/shc_x.so", 1);

	if (pid == 0)
	{

		// Start tracing to protect from dump & trace
		if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0)
		{
			kill(getpid(), SIGKILL);
			_exit(1);
		}

		// Decode Bash
		while (len > 0)
		{
			indx++;
			tmp = stte[indx];
			jndx += tmp;
			stte[indx] = stte[jndx];
			stte[jndx] = tmp;
			tmp += stte[indx];
			*ptr ^= stte[tmp];
			ptr++;
			len--;
		}

		// Do the magic
		sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

		// Exec bash script //fork execl with 'sh -c'
		system(tmp2);

		// Empty script variable
		memcpy(tmp2, str, lentmp);

		// Clean temp
		remove("/tmp/shc_x.so");

		// Sinal to detach ptrace
		ptrace(PTRACE_DETACH, 0, 0, 0);
		exit(0);
	}
	else
	{
		wait(&status);
	}

	/* Seccomp Sandboxing - Start */
	seccomp_hardening();

	exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants.
 */
int key_with_file(char *file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char *sh11, int argc, char **argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv)
	{
		fprintf(stderr, "argv=<null>\n");
	}
	else
	{
		for (i = 0; i <= argc; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char **argv, char *arg)
{
	for (; argv && *argv && *argv != arg; argv++)
		;
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char *string;
	extern char **environ;

	mask = (unsigned long)getpid();
	stte_0();
	key(&chkenv, (void *)&chkenv_end - (void *)&chkenv);
	key(&data, sizeof(data));
	key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string)
	{
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask)
	{
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void) {}

#if HARDENING

static void gets_process_name(const pid_t pid, char *name)
{
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE *f = fopen(procfile, "r");
	if (f)
	{
		size_t size;
		size = fread(name, sizeof(char), sizeof(procfile), f);
		if (size > 0)
		{
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening()
{
	prctl(PR_SET_DUMPABLE, 0);
	prctl(PR_SET_PTRACER, -1);

	int pid = getppid();
	char name[256] = {0};
	gets_process_name(pid, name);

	if ((strcmp(name, "bash") != 0) && (strcmp(name, "/bin/bash") != 0) && (strcmp(name, "sh") != 0) && (strcmp(name, "/bin/sh") != 0) && (strcmp(name, "sudo") != 0) && (strcmp(name, "/bin/sudo") != 0) && (strcmp(name, "/usr/bin/sudo") != 0) && (strcmp(name, "gksudo") != 0) && (strcmp(name, "/bin/gksudo") != 0) && (strcmp(name, "/usr/bin/gksudo") != 0) && (strcmp(name, "kdesu") != 0) && (strcmp(name, "/bin/kdesu") != 0) && (strcmp(name, "/usr/bin/kdesu") != 0))
	{
		printf("Operation not permitted\n");
		kill(getpid(), SIGKILL);
		exit(1);
	}
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
#if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
#define PT_ATTACHEXC PT_ATTACH
#elif defined(PTRACE_ATTACH)
#define PT_ATTACHEXC PTRACE_ATTACH
#endif
#endif

void untraceable(char *argv0)
{
	char proc[80];
	int pid, mine;

	switch (pid = fork())
	{
	case 0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as", (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR | O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine)
		{
			kill(pid, SIGCONT);
		}
		else
		{
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char *xsh(int argc, char **argv)
{
	char *scrpt;
	int ret, i, j;
	char **varg;
	char *me = argv[0];
	if (me == NULL)
	{
		me = getenv("_");
	}
	if (me == 0)
	{
		fprintf(stderr, "E: neither argv[0] nor $_ works.");
		exit(1);
	}

	ret = chkenv(argc);
	stte_0();
	key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date) < time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret)
	{
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
		arc4_hardrun(text, text_z);
		exit(0);
		/* Seccomp Sandboxing - Start */
		seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int)' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	}
	else
	{ /* Reexecute */
		if (*xecc)
		{
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		}
		else
		{
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0]; /* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts; /* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo; /* Option introducing inline code */
	varg[j++] = scrpt;	  /* The script itself */
	if (*lsto)
		varg[j++] = lsto;	 /* Option meaning last option */
	i = (ret > 1) ? ret : 0; /* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++]; /* Main run-time arguments */
	varg[j] = 0;			   /* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char **argv)
{
#if SETUID
	setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
			errno ? ": " : "",
			errno ? strerror(errno) : "",
			argv[1] ? argv[1] : "<null>");
	return 1;
}
