#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -f VisionVoyageServer.sh -o VisionVoyageServer
#endif

static char data[] =
#define pswd_z 256
#define pswd ((&data[29]))
	"\247\222\001\175\075\055\222\371\223\152\007\170\207\102\033\216"
	"\071\272\266\200\301\276\151\227\304\142\074\374\160\031\216\133"
	"\264\074\252\046\252\075\171\252\241\332\177\257\060\047\136\135"
	"\011\142\252\037\346\216\024\052\316\021\062\370\052\300\123\337"
	"\375\376\005\250\073\177\123\334\131\323\214\212\372\353\347\004"
	"\115\222\044\064\041\070\137\357\111\221\350\163\122\073\122\120"
	"\072\130\370\165\327\114\122\061\037\337\273\032\312\243\036\030"
	"\065\102\115\127\172\254\107\304\075\057\067\220\153\212\340\245"
	"\343\331\033\273\045\156\354\105\115\247\140\030\112\176\060\200"
	"\301\175\327\074\052\036\000\150\115\070\370\271\302\331\137\246"
	"\262\172\141\330\350\116\036\066\365\176\116\077\375\176\300\276"
	"\374\227\372\046\266\373\217\004\063\207\275\366\140\034\234\023"
	"\227\376\354\177\114\012\266\101\211\004\201\206\203\102\105\200"
	"\331\100\247\217\073\066\223\156\276\121\144\037\156\001\062\005"
	"\000\036\205\114\051\073\216\262\077\020\071\303\122\176\103\054"
	"\277\352\274\372\041\120\151\337\241\316\376\017\317\061\024\320"
	"\120\232\034\171\325\253\054\025\273\145\330\016\344\034\072\243"
	"\007\367\236\050\107\007\007\351\325\006\370\245\067\262\203\027"
	"\104\205\224\201\263\047\173\106\221\202\277\030\305\333\247\376"
	"\225\135\176\126\034\350\355\340\113\052\334\273\334\140\323"
#define msg1_z 65
#define msg1 ((&data[334]))
	"\242\230\217\035\337\040\240\236\071\145\171\341\144\017\076\341"
	"\060\041\235\067\234\111\226\327\076\361\052\177\036\224\376\231"
	"\365\042\066\210\064\301\137\170\136\333\263\245\021\325\015\013"
	"\103\264\246\327\331\217\034\132\261\014\054\126\262\273\146\323"
	"\222\351\347\241\312\137\203\033\074\174\165\052\210\031\142\260"
	"\343\145\132\313\123\073"
#define date_z 1
#define date ((&data[405]))
	"\077"
#define text_z 459
#define text ((&data[446]))
	"\245\172\136\015\034\367\234\072\327\275\333\166\367\100\360\330"
	"\244\377\026\207\144\161\123\267\255\152\065\305\074\216\076\341"
	"\010\234\356\045\224\212\137\154\225\336\225\105\000\224\065\263"
	"\320\151\204\222\336\043\003\205\033\161\206\206\273\031\074\376"
	"\313\370\265\105\034\272\052\076\164\020\023\074\355\316\113\005"
	"\071\051\135\251\317\222\103\171\227\154\121\330\376\172\306\145"
	"\356\221\002\375\076\206\216\254\073\011\307\034\022\107\330\255"
	"\123\055\015\147\020\355\334\236\361\247\262\150\272\124\343\334"
	"\146\153\107\035\352\134\333\004\374\165\217\155\346\371\373\042"
	"\143\211\337\111\224\132\165\021\307\214\125\152\075\300\217\077"
	"\245\040\227\212\135\241\014\113\116\173\200\132\245\014\021\064"
	"\264\360\117\057\053\311\050\001\116\153\177\212\344\312\105\177"
	"\020\230\275\060\171\046\236\360\327\254\062\060\000\107\065\276"
	"\173\123\131\313\142\071\066\320\047\105\052\077\301\221\377\052"
	"\251\043\232\353\221\237\135\154\100\101\302\322\307\147\015\111"
	"\331\372\237\345\355\240\247\251\211\265\122\243\221\350\120\065"
	"\141\250\230\043\103\246\100\367\212\127\005\332\006\043\257\005"
	"\025\257\106\203\264\220\023\254\344\123\225\316\053\032\073\331"
	"\226\263\357\171\047\213\170\356\121\042\275\044\264\354\025\342"
	"\255\120\304\015\046\243\203\115\135\317\062\255\260\252\151\215"
	"\246\315\013\266\342\134\131\205\041\104\240\314\314\272\257\250"
	"\045\350\272\311\171\062\026\110\365\372\242\001\053\165\370\126"
	"\103\101\341\313\363\105\065\063\355\161\216\075\373\067\320\202"
	"\111\237\200\264\073\233\335\065\052\226\245\254\001\073\265\225"
	"\006\140\060\175\374\035\075\177\166\350\313\223\003\145\371\025"
	"\137\256\070\332\330\046\013\057\003\256\315\146\107\240\320\154"
	"\272\320\313\046\115\027\113\220\150\302\053\010\340\114\324\105"
	"\060\141\061\355\217\350\230\230\131\161\306\077\055\354\152\371"
	"\055\000\031\342\353\063\110\167\053\140\374\162\261\213\320\157"
	"\050\317\305\304\342\332\153\251\363\116\051\171\127\152\032\337"
	"\173\312\020\265\376\265\264\251\223\020\135\245\303\330\250\241"
	"\035\112\304\107\072\342\076\173\322\026\040\321\055\247\066\237"
	"\373\356\114\145\043\021\241\262\117\203\272\354\161\337\201\374"
	"\077\355\103\172\317\202\365\242\231\025\163\306\275\252\145\270"
	"\230\262\035\274\303\277\156\023\102"
#define shll_z 14
#define shll ((&data[959]))
	"\156\132\043\062\264\042\056\131\035\016\134\123\025\127\011\201"
#define rlax_z 1
#define rlax ((&data[975]))
	"\121"
#define chk2_z 19
#define chk2 ((&data[979]))
	"\166\267\340\124\257\014\346\372\220\101\357\013\167\076\375\155"
	"\216\351\375\030\350\033\020"
#define tst2_z 19
#define tst2 ((&data[1000]))
	"\212\114\345\206\015\341\031\360\227\310\115\236\357\244\340\164"
	"\235\224\017\176\377\074\103\230"
#define inlo_z 3
#define inlo ((&data[1023]))
	"\364\044\115"
#define opts_z 1
#define opts ((&data[1026]))
	"\007"
#define xecc_z 15
#define xecc ((&data[1028]))
	"\306\065\266\345\316\152\251\020\223\216\005\310\316\131\235\101"
	"\027\365\110"
#define lsto_z 1
#define lsto ((&data[1046]))
	"\001"
#define tst1_z 22
#define tst1 ((&data[1047]))
	"\072\276\141\036\270\374\207\066\140\300\252\345\342\320\073\142"
	"\017\342\103\057\242\137\063\270\326\103\205"
#define chk1_z 22
#define chk1 ((&data[1074]))
	"\036\153\024\222\271\152\137\314\367\000\102\150\154\133\120\246"
	"\271\300\304\221\307\075"
#define msg2_z 19
#define msg2 ((&data[1097]))
	"\302\073\144\375\055\373\360\342\040\032\304\205\221\216\071\160"
	"\357\344\243\000\105" /* End of data[] */;
#define hide_z 4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC 0 /* Define as 1 to debug execvp calls */
#define TRACEABLE 1 /* Define as 1 to enable ptrace the executable */
#define HARDENING 0 /* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON 0 /* Define as 1 to enable work with busybox */

#if HARDENING
static const char *shc_x[] = {
	"/*",
	" * Copyright 2019 - Intika <intika@librefox.org>",
	" * Replace ******** with secret read from fd 21",
	" * Also change arguments location of sub commands (sh script commands)",
	" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
	" */",
	"",
	"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
	"#define PLACEHOLDER \"********\"",
	"#include <dlfcn.h>",
	"#include <stdlib.h>",
	"#include <string.h>",
	"#include <unistd.h>",
	"#include <stdio.h>",
	"#include <signal.h>",
	"",
	"static char secret[128000]; //max size",
	"typedef int (*pfi)(int, char **, char **);",
	"static pfi real_main;",
	"",
	"// copy argv to new location",
	"char **copyargs(int argc, char** argv){",
	"    char **newargv = malloc((argc+1)*sizeof(*argv));",
	"    char *from,*to;",
	"    int i,len;",
	"",
	"    for(i = 0; i<argc; i++){",
	"        from = argv[i];",
	"        len = strlen(from)+1;",
	"        to = malloc(len);",
	"        memcpy(to,from,len);",
	"        // zap old argv space",
	"        memset(from,'\\0',len);",
	"        newargv[i] = to;",
	"        argv[i] = 0;",
	"    }",
	"    newargv[argc] = 0;",
	"    return newargv;",
	"}",
	"",
	"static int mymain(int argc, char** argv, char** env) {",
	"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
	"    return real_main(argc, copyargs(argc,argv), env);",
	"}",
	"",
	"int __libc_start_main(int (*main) (int, char**, char**),",
	"                      int argc,",
	"                      char **argv,",
	"                      void (*init) (void),",
	"                      void (*fini)(void),",
	"                      void (*rtld_fini)(void),",
	"                      void (*stack_end)){",
	"    static int (*real___libc_start_main)() = NULL;",
	"    int n;",
	"",
	"    if (!real___libc_start_main) {",
	"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
	"        if (!real___libc_start_main) abort();",
	"    }",
	"",
	"    n = read(21, secret, sizeof(secret));",
	"    if (n > 0) {",
	"      int i;",
	"",
	"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
	"    for (i = 1; i < argc; i++)",
	"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
	"          argv[i] = secret;",
	"    }",
	"",
	"    real_main = main;",
	"",
	"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
	"}",
	"",
	0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte.
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do
	{
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once.
 */
void key(void *str, int len)
{
	unsigned char tmp, *ptr = (unsigned char *)str;
	while (len > 0)
	{
		do
		{
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data.
 */
void arc4(void *str, int len)
{
	unsigned char tmp, *ptr = (unsigned char *)str;
	while (len > 0)
	{
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall)                                        \
	BPF_JUMP(BPF_JMP + BPF_JEQ + BPF_K, SYS_##syscall, 0, 1), \
		BPF_STMT(BPF_RET + BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
	/* validate arch */
	BPF_STMT(BPF_LD + BPF_W + BPF_ABS, ArchField),
	BPF_JUMP(BPF_JMP + BPF_JEQ + BPF_K, AUDIT_ARCH_X86_64, 1, 0),
	BPF_STMT(BPF_RET + BPF_K, SECCOMP_RET_KILL),

	/* load syscall */
	BPF_STMT(BPF_LD + BPF_W + BPF_ABS, offsetof(struct seccomp_data, nr)),

	/* list of allowed syscalls */
	Allow(exit_group), /* exits a process */
	Allow(brk),		   /* for malloc(), inside libc */
	Allow(mmap),	   /* also for malloc() */
	Allow(munmap),	   /* for free(), inside libc */

	/* and if we don't match above, die */
	BPF_STMT(BPF_RET + BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
	.len = sizeof(filter) / sizeof(filter[0]),
	.filter = filter};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening()
{
	if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0))
	{
		perror("Could not start seccomp:");
		exit(1);
	}
	if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1)
	{
		perror("Could not start seccomp:");
		exit(1);
	}
}
/* End Seccomp Sandboxing Init */

void shc_x_file()
{
	FILE *fp;
	int line = 0;

	if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL)
	{
		exit(1);
		exit(1);
	}
	for (line = 0; shc_x[line]; line++)
		fprintf(fp, "%s\n", shc_x[line]);
	fflush(fp);
	fclose(fp);
}

int make()
{
	char *cc, *cflags, *ldflags;
	char cmd[4096];

	cc = getenv("CC");
	if (!cc)
		cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd))
	{
		remove("/tmp/shc_x.c");
		return -1;
	}
	remove("/tmp/shc_x.c");
	return 0;
}

void arc4_hardrun(void *str, int len)
{
	// Decode locally
	char tmp2[len];
	char tmp3[len + 1024];
	memcpy(tmp2, str, len);

	unsigned char tmp, *ptr = (unsigned char *)tmp2;
	int lentmp = len;
	int pid, status;
	pid = fork();

	shc_x_file();
	if (make())
	{
		exit(1);
	}

	setenv("LD_PRELOAD", "/tmp/shc_x.so", 1);

	if (pid == 0)
	{

		// Start tracing to protect from dump & trace
		if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0)
		{
			kill(getpid(), SIGKILL);
			_exit(1);
		}

		// Decode Bash
		while (len > 0)
		{
			indx++;
			tmp = stte[indx];
			jndx += tmp;
			stte[indx] = stte[jndx];
			stte[jndx] = tmp;
			tmp += stte[indx];
			*ptr ^= stte[tmp];
			ptr++;
			len--;
		}

		// Do the magic
		sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

		// Exec bash script //fork execl with 'sh -c'
		system(tmp2);

		// Empty script variable
		memcpy(tmp2, str, lentmp);

		// Clean temp
		remove("/tmp/shc_x.so");

		// Sinal to detach ptrace
		ptrace(PTRACE_DETACH, 0, 0, 0);
		exit(0);
	}
	else
	{
		wait(&status);
	}

	/* Seccomp Sandboxing - Start */
	seccomp_hardening();

	exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants.
 */
int key_with_file(char *file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char *sh11, int argc, char **argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv)
	{
		fprintf(stderr, "argv=<null>\n");
	}
	else
	{
		for (i = 0; i <= argc; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char **argv, char *arg)
{
	for (; argv && *argv && *argv != arg; argv++)
		;
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char *string;
	extern char **environ;

	mask = (unsigned long)getpid();
	stte_0();
	key(&chkenv, (void *)&chkenv_end - (void *)&chkenv);
	key(&data, sizeof(data));
	key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string)
	{
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask)
	{
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void) {}

#if HARDENING

static void gets_process_name(const pid_t pid, char *name)
{
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE *f = fopen(procfile, "r");
	if (f)
	{
		size_t size;
		size = fread(name, sizeof(char), sizeof(procfile), f);
		if (size > 0)
		{
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening()
{
	prctl(PR_SET_DUMPABLE, 0);
	prctl(PR_SET_PTRACER, -1);

	int pid = getppid();
	char name[256] = {0};
	gets_process_name(pid, name);

	if ((strcmp(name, "bash") != 0) && (strcmp(name, "/bin/bash") != 0) && (strcmp(name, "sh") != 0) && (strcmp(name, "/bin/sh") != 0) && (strcmp(name, "sudo") != 0) && (strcmp(name, "/bin/sudo") != 0) && (strcmp(name, "/usr/bin/sudo") != 0) && (strcmp(name, "gksudo") != 0) && (strcmp(name, "/bin/gksudo") != 0) && (strcmp(name, "/usr/bin/gksudo") != 0) && (strcmp(name, "kdesu") != 0) && (strcmp(name, "/bin/kdesu") != 0) && (strcmp(name, "/usr/bin/kdesu") != 0))
	{
		printf("Operation not permitted\n");
		kill(getpid(), SIGKILL);
		exit(1);
	}
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
#if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
#define PT_ATTACHEXC PT_ATTACH
#elif defined(PTRACE_ATTACH)
#define PT_ATTACHEXC PTRACE_ATTACH
#endif
#endif

void untraceable(char *argv0)
{
	char proc[80];
	int pid, mine;

	switch (pid = fork())
	{
	case 0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as", (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR | O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine)
		{
			kill(pid, SIGCONT);
		}
		else
		{
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char *xsh(int argc, char **argv)
{
	char *scrpt;
	int ret, i, j;
	char **varg;
	char *me = argv[0];
	if (me == NULL)
	{
		me = getenv("_");
	}
	if (me == 0)
	{
		fprintf(stderr, "E: neither argv[0] nor $_ works.");
		exit(1);
	}

	ret = chkenv(argc);
	stte_0();
	key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date) < time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret)
	{
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
		arc4_hardrun(text, text_z);
		exit(0);
		/* Seccomp Sandboxing - Start */
		seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int)' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	}
	else
	{ /* Reexecute */
		if (*xecc)
		{
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		}
		else
		{
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0]; /* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts; /* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo; /* Option introducing inline code */
	varg[j++] = scrpt;	  /* The script itself */
	if (*lsto)
		varg[j++] = lsto;	 /* Option meaning last option */
	i = (ret > 1) ? ret : 0; /* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++]; /* Main run-time arguments */
	varg[j] = 0;			   /* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char **argv)
{
#if SETUID
	setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
			errno ? ": " : "",
			errno ? strerror(errno) : "",
			argv[1] ? argv[1] : "<null>");
	return 1;
}
